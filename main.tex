\documentclass[margin=1mm]{standalone}

\usepackage
  { tikz-cd
  , graphicx
  , amsmath
  , amssymb
  , amsfonts
  , xcolor
  , commutative-diagrams
  }
\usepackage[all,2cell]{xy}\UseAllTwocells
\def\uhom{\underline{\hom}}

\usetikzlibrary{decorations.markings}
\tikzset{mid vert/.style={/utils/exec=\tikzset{every node/.append style={outer sep=0.8ex}},
			postaction=decorate,decoration={markings,
					mark=at position 0.5 with {\draw[-] (0,#1) -- (0,-#1);}}},
	mid vert/.default=0.75ex}
%--
\DeclareRobustCommand{\proarrow}{%
	\mathrel{
		\mkern-15.25mu
		\begin{tikzcd}[row sep=2.7em, column sep=1.35em, ampersand replacement=\&]
			{}
			%\arrow[r, "\mapstochar", pos=0.45, outer sep=-3.0pt] \&
			\arrow[r, mid vert] \&
			{}
		\end{tikzcd}
		\mkern-16.25mu
	}
}
\let\pto\proarrow
\def\r{\text{r}}
\def\leib#1#2{\partial #1 \otimes #2 + #1 \otimes \partial #2}
\begin{document}
\xymatrix{
&E\ar[dr]\ar[dl]& \\
S(X\rhd M,X\rhd M)\ar[d] && S(M,M) \ar[d]\\
S(X^*\rhd (X\rhd M),M) \ar[rr]&& S((X^*\otimes X)\rhd M,M)
}
\end{document}
